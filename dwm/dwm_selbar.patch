diff -r ba5be5b023e1 dwm.c
--- a/dwm.c	Sat Mar 17 18:14:17 2012 +0100
+++ b/dwm.c	Thu Mar 22 11:17:08 2012 +0100
@@ -754,7 +754,7 @@
 	if((dc.w = dc.x - x) > bh) {
 		dc.x = x;
 		if(m->sel) {
-			col = m == selmon ? dc.sel : dc.norm;
+			col = m == selmon ? dc.norm : dc.norm;
 			drawtext(m->sel->name, col, False);
 			drawsquare(m->sel->isfixed, m->sel->isfloating, False, col);
 		}
